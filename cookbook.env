# The one local Ollama model every Phase 1 lesson runs on. To rerun the whole
# tour on a different model, change this single line and pull the model first
# (`ollama pull <model>`). A recipe's --model flag, or an OLLAMA_MODEL env
# var, still overrides this per run.
OLLAMA_MODEL=llama3.2:1b
