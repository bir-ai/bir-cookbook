[project]
name = "bir-cookbook-ollama-05-governance"
version = "0.0.0"
description = "Bir cookbook · Phase 1 Lesson 05 — governance: tags, kill switch, sampling, redaction, and capture limits on local Ollama."
requires-python = ">=3.11"
dependencies = [
    "bir-sdk==0.3.0",
    "ollama>=0.3",
]

[tool.uv]
# Runnable app, not an installable library — no build backend needed.
package = false
