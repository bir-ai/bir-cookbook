[project]
name = "bir-cookbook-ollama-04-async-streaming"
version = "0.0.0"
description = "Bir cookbook · Phase 1 Lesson 04 — async tracing, token streaming, and observed generators on local Ollama."
requires-python = ">=3.11"
dependencies = [
    "bir-sdk==0.3.0",
    "ollama>=0.3",
]

[tool.uv]
# Runnable app, not an installable library — no build backend needed.
package = false
