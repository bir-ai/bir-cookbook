[project]
name = "bir-cookbook-ollama-08-evals"
version = "0.0.0"
description = "Bir cookbook · Phase 1 Lesson 08 — evals: datasets, deterministic evaluators, run_experiment, reports, and the compare_experiments regression gate, on local Ollama."
requires-python = ">=3.11"
dependencies = [
    "bir-sdk==0.3.0",
    "ollama>=0.3",
]

[tool.uv]
# Runnable app, not an installable library — no build backend needed.
package = false
