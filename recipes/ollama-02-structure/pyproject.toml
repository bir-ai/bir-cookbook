[project]
name = "bir-cookbook-ollama-02-structure"
version = "0.0.0"
description = "Bir cookbook · Phase 1 Lesson 02 — nested spans, tools, retrieval, and scores around a local Ollama call."
requires-python = ">=3.11"
dependencies = [
    "bir-sdk==0.3.0",
    "ollama>=0.3",
]

[tool.uv]
# Runnable app, not an installable library — no build backend needed.
package = false
