# Copy to .env (git-ignored) or `export` in your shell. Env vars only — never
# commit real secrets.

# Phase 1 (local Ollama) needs NO key — it talks to a local server, so a recipe
# built on Ollama may not need a .env at all.
#
# TODO (Phase 2 keyed free providers): the env var this recipe needs, e.g.
# MISTRAL_API_KEY=
# GROQ_API_KEY=
